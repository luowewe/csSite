<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta content="width=device-width,initial-scale=1" name="viewport">
  <title>{{ metadata.title }}</title>
  <link href="/css/homepage.css" media="all" rel="stylesheet" type="text/css">
  <script src="/js/toc.js" defer></script>
</head>
<body>
  <div>
    <p class="logo"><a href="/">{{ metadata.title }}</a></p>

    {{ content | safe }}

    <p class="hash">
      {{ buildinfo.time.human }} / {{ buildinfo.hash }}{% if buildinfo.dirty %} (dirty){% endif %}
    </p>
  </div>
</body>
</html>
