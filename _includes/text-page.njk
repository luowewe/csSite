<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta content="width=device-width,initial-scale=1" name="viewport">
  <title>{{ title }}</title>
  <link href="/css/{{ style }}.css" media="all" rel="stylesheet" type="text/css">
</head>
<body>
  <div>
    <p class="logo"><a href="/">{{ metadata.title }}</a></p>

    <h1>{{ title }}</h1>

    {{ content | safe }}

  </div>
</body>
</html>
